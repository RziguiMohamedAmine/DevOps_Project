version: '2'
services:
  devops-project:
    container_name: devops
    build: ./
    restart: on-failure
    ports:
      - "8082:8082"
   # depends_on:
    #  - mysqlDev-container
    volumes:
      - .m2:/root/.m2

volumes:
  sqldata:
