version: '2'
services:
  mysqldb:
    container_name: mysqldb
    image: mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    volumes:
      - db:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
  devops-project:
    container_name: my-spring-app
    depends_on:
      - mysqldb
    build: ./
    restart: on-failure
    ports:
      - "8083:8083"
    volumes:
      - .m2:/root/.m2
    stdin_open: true
    tty: true
volumes:
  db:
